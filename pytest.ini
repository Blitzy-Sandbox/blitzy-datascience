[pytest]
testpaths = tests
markers =
    integration: marks live NBA Stats API tests (deselect with '-m "not integration"')
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
